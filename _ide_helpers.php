<?php

declare(strict_types=1);

namespace Honed\Refine {

    /**
     * @method \Illuminate\Contracts\Pagination\LengthAwarePaginator paginate(int|null $perPage = null, array $columns = ['*'], string $pageName = 'page', int|null $page = null) Paginate the given query.
     * @method \Illuminate\Database\Eloquent\Collection get(array|string $columns = ['*']) Execute the query as a "select" statement.
     * @method \Illuminate\Contracts\Pagination\Paginator simplePaginate(int|null $perPage = null, array $columns = ['*'], string $pageName = 'page', int|null $page = null) Paginate the given query into a simple paginator.
     * @method \Illuminate\Support\LazyCollection cursor() Execute the query as a cursor, using a generator to avoid memory issues.
     */
    class Refine {}
}
